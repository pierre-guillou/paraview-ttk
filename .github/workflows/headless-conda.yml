name: headless_conda

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:

    - uses: actions/checkout@v6
      name: Checkout TTK-ParaView source code

    - uses: prefix-dev/setup-pixi@v0.9.6
      with:
        run-install: false

    - name: Create ParaView package
      run: |
        pixi publish

    - name: Upload install executable
      uses: actions/upload-artifact@v7
      with:
        name: ttk-paraview-${{ matrix.os }}
        path: "*.conda"

  test:
    needs: build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:

    - name: Download artifacts
      uses: actions/download-artifact@v8
      with:
        name: ttk-paraview-${{ matrix.os }}

    - uses: prefix-dev/setup-pixi@v0.9.6
      with:
        run-install: false

    - name: Install package
      shell: bash
      run: |
        pixi init
        pixi add $PWD/*.conda
        pixi tree

    - name: Test package
      run: |
        pixi run pvpython --version

    - name: Test pvpython vtk import
      shell: pixi run bash -e {0}
      run: |
        pvpython -c "import vtk"

    - name: Test pvpython paraview import
      shell: pixi run bash -e {0}
      run: |
        pvpython -c "from paraview.simple import *"
